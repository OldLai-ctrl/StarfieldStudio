# Starfield Studio 2.6 使用说明

面向部分工业黑白相机的实时采集、图像校正和直播软件。运行 `StarfieldStudio-2.6.exe`，保留同目录依赖文件夹。相机接入方法见 `SDK接入说明.md`。

程序使用深色星野镜头图标，窗口、任务栏和桌面快捷方式会显示同一图标。

## 采集与测光

选择工业黑白相机及 TUCam、海康 MVS 或图谱 ToupCam 接口，指定接口文件与设备序号。连接后读取真实参数范围。模拟星野模式无需设备。

曝光以毫秒显示。增益单位来自接口，不同品牌不能直接比较。连接后“输入位深”列出该 SDK 实际提供的 Mono 模式；切换时会停止并重新启动采集，随后读回并校验位深。海康画幅为相机 ROI 裁切，其他接口按设备提供的分辨率列表工作；软件 Binning 对收到的完整图像做平均缩小。

自动调节可选优先曝光时长、优先增益、手动。优先项达到边界后才调另一项。“锁定曝光时间（高优先级）”或“锁定增益（高优先级）”会阻止自动曝光改动对应参数，两项都锁定时保持当前值。直接修改曝光或增益也切回手动。目标亮度使用原始图像第 90 百分位及接口位深对应量程，不受拉伸与伪彩影响。

点击框选测光区域后拖出矩形，隐藏选框仍使用该区域测光；恢复全画面才清除区域。边框不会进入 OBS。

## 校正与叠加

遮光拍摄偏置和暗场，对均匀光源拍摄平场，或加载校正库。不同相机、位深、增益、画幅与不匹配曝光的校正帧不可混用。开启校正时，自动调节受可匹配校正组合限制。

叠加可选关闭、平均、积分、目标亮度和最大值。窗口可以按最近设定秒数或最近设定帧数限制，始终只保留选定范围内的帧。平均保持平均亮度，积分随帧数增加；目标亮度从最新帧向前取最短的积分后缀，使叠加结果平均亮度达到“叠加目标亮度 / %”，窗口时长或帧数是上限，达不到目标时使用全部窗口；最大值逐像素保留窗口内最亮帧，因此适合保留星点和流星。存储按曝光归一化，输出还原到当前曝光尺度。分辨率、相机 Binning、输入位深、校正组合或计算设备变化会清空旧帧。画面突变、自动增益或手动增益变化会保留有限窗口并自然移出旧帧，避免直播叠加突然从头开始；增益改变后的过渡帧可能具有不同放大倍率，若需要严格同一标度，可手动清空一次。

亮度触发器可选“平均亮度低于”或“平均亮度高于”，阈值以当前 Mono 位深满量程的百分比输入。它使用当前窗口全部帧的平均亮度；达到条件时切换到指定的平均、积分、目标亮度、最大值或单帧模式，状态会显示在右侧。

“计算设备”可选“自动（优先 GPU）”“CPU”或“GPU（OpenCL）”。自动模式会使用显卡驱动提供的 OpenCL，主流 NVIDIA 显卡和 Intel 核显均可使用各自的 OpenCL 运行时；手动选择 GPU 时也会检查运行时。选用 GPU 后，校正、软件 Binning、滚动窗口、像素数学、弱光、即时降噪、镜像、对比度、曲线和锐化等可并行的图像处理会使用 OpenCL；右侧“GPU处理 N 次”显示实际调用次数。直方图统计、自动曝光测光、伪彩映射、图片编码和相机接口仍使用 CPU。Windows 任务管理器的“3D”图表可能不显示 OpenCL，请查看 GPU 引擎或 Compute/OpenCL 图表。驱动不可用或采集中发生 OpenCL 错误时，软件会把已有窗口迁移回 CPU 并继续采集；右侧状态和运行记录会显示“CPU 回退”。

“校正 / 显示”页的“画面方向”提供左右镜像和上下镜像，只改变预览、OBS 和预览 PNG；原始文件、校正帧和滚动数据保持相机原始方向。测光和像素运算选框会自动换算回原始坐标，镜像后无需手动修正。左侧每个大项标题都可以点击折叠，折叠只收起界面内容，不改变参数。

关闭叠加且无窗口运算时不缓存多帧；关闭全局叠加但使用局部窗口运算时只缓存选区。自动内存预算在保留系统余量时增长，资源不足仍需缩短窗口或降低尺寸。

## 显示与像素运算

默认固定显示范围，可使用一次性拉伸或持续自动拉伸。持续拉伸可能使曝光或积分变化不明显。原始直方图按接口位深显示，处理后包含负值与超量程，显示灰度为伪彩前 0—255；纵轴为对数。右侧“原始峰值”只针对单帧输入，“处理均值”可能来自浮点滚动积分，不能把两者当成同一量程。

自定义伪彩支持 2—64 个灰度—颜色控制点，双击颜色格打开取色器。控制点之间插值，范围外取端点颜色。此模式按实际处理灰度映射，不再经过显示黑点、白点和伽马；映射随配置保存。

实时图像调整位于“校正 / 显示”页，仅作用于预览、OBS 纯画面和预览 PNG，原始 TIFF、SER 与处理后浮点文件不变。对比度滑块范围为 −100% 至 +100%；锐化强度为 0—300%，锐化半径为 0.1—20 像素，均可拖动后用右侧数值微调。锐化使用实时反遮罩，半径越大影响范围越宽。

曲线模式有“关闭”“Camera Raw 参数曲线”和“点曲线”。参数曲线提供阴影、暗部、亮部、高光四个连续滑块，适合直播中快速压暗部或提亮星云；“编辑点曲线…”可用输入—输出百分比控制点自定义曲线，端点必须覆盖 0% 和 100%。曲线预览会随滑块实时刷新。调整顺序为亮暗点 / 伽马、对比度、曲线、锐化，再进入伪彩映射。

即时降噪在“校正 / 显示”页中调整，只有中值 3×3（去孤立噪点）和高斯 3×3（轻度平滑）两种低开销滤波，强度从0到100%连续可调。它只作用于预览、OBS 和预览 PNG，不改变原始帧或处理后浮点文件，也不增加滚动缓存。中值对孤立热像素更有效，但可能削弱单像素星点和流星；使用最大值模式时应关闭或降低强度。

弱光增强预设在同一页中调整，强度从0到100%连续可调。自适应弱光提亮使用当前画面的稳健黑白点估计提升暗部；弱光提亮 + 轻度降噪再加一次 3×3 高斯滤波；星点/流星保护只提升暗部，尽量保持亮点和超出统计白点的亮线。预设只作用于预览、OBS 和预览 PNG，不改变原始或处理后浮点数据，也不增加滚动缓存。它是根据公开弱光 ISP 思路制作的实时近似，不是任何厂商闭源臻全彩算法的复制。需要时间方向的降噪时使用滚动平均／积分；流星监控建议窗口最大值配合较低的降噪强度。

像素运算支持常数加法、系数乘法、窗口平均/积分、加减窗口平均、参考帧加减及平均、幂律、对数、平方根、绝对值、限制范围。一次选一种，可用画面区域与灰度上下限共同限制。阈值依据当前运算前像素，不逐帧剔除历史值。操作在校正及全局叠加后、伪彩前；未选像素保持原值。

参考帧记录运算前图像，不自动更新，不随配置保存；重启后需重新记录。幂律指数用“常数/系数/指数”，非线性操作使用“灰度尺度”；对数、平方根和幂律保留负数符号。

## OBS 与保存

打开 OBS 纯画面，在 OBS 中添加窗口采集，再按需开启虚拟摄像头。正常等待下一次曝光时保留最后完整画面，不会把旧图重复加入叠加。暂停或出错有提示，断开连接清空画面。长曝光仍决定真实采集帧率。

原始 TIFF / SER 保存原始灰度值，使用 16 位容器，位深信息在 TIFF 元数据或 SER 配套 JSON 中。较低位深不会增加信息。浮点 TIFF/FITS 保存处理后负值与积分范围，PNG/OBS 是 8 位 RGB 预览。配置保存接口选择及处理参数，校正库需另存。

本版完成模拟采集和接口契约测试，未完成三品牌实机及实际 OBS 推流验证。
